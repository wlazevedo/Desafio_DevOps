apiVersion: v1
kind: ConfigMap
metadata:
  name: loki-dashboard
  namespace: monitoring
  labels:
    grafana_dashboard: "1"
data:
  loki-dashboard.json: |
    {
      "title": "Logs - Whoami",
      "panels": [
        {
          "type": "logs",
          "title": "Whoami Logs",
          "targets": [
            {
              "expr": "{namespace=\"devops-challenge\", app=\"whoami\"}"
            }
          ]
        }
      ],
      "schemaVersion": 36
    }
